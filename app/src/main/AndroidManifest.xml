<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srikiransistla.homework5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/su"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_task2"
            android:label="Task 2"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeOrange">
        </activity>
        <activity android:name=".Activity_task3"
            android:label="Task 3"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeRed">
        </activity>
    </application>

</manifest>
